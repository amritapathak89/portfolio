# Portfolio Website

A personal portfolio — a single **static** page showcasing education, experience, skills, and selected work, with a contact email.

## Stack

- **HTML + vanilla JavaScript** — no framework, no backend.
- **Tailwind CSS v4** (`@tailwindcss/cli`) is the only build step: it compiles
  `frontend/src/input.css` → `frontend/public/output.css`.
- **Bootstrap Icons** + **Oswald** font, loaded via CDN in `index.html`.
- A custom `script.js` provides the starfield canvas, image carousel, scroll/fade
  animations, navbar toggle, and the contact-email tooltip.

## Project structure

```
frontend/
├── package.json     # scripts: dev, build (Tailwind CLI)
├── src/
│   └── input.css    # Tailwind entry: @theme tokens, @keyframes, base layer
└── public/          # the deployable static site
    ├── index.html   # the page
    ├── output.css   # GENERATED by Tailwind — do not edit by hand
    ├── script.js    # starfield, carousel, animations, navbar, email tooltip
    ├── robots.txt / sitemap.xml
    └── assets/images/
```

## Running locally

Install once, then build (or watch) the CSS and serve the `public/` folder.
The repo's `Makefile` wraps the common tasks:

```bash
make install   # cd frontend && npm install
make build     # one-off minified output.css
make dev       # watch & rebuild CSS on change
make serve     # python3 -m http.server --directory frontend/public 8080
```

Once `output.css` exists the site is fully static — you can also just open
`frontend/public/index.html` in a browser. Visit http://localhost:8080 when serving.

> **Note:** `output.css` is generated. Never edit it by hand — change Tailwind
> utility classes in `index.html` or tokens/keyframes in `src/input.css`, then
> rebuild with `make build`.

## Deployment

Serve the contents of `frontend/public/` from any static host or web server (e.g. Apache from `/var/www/html`). No server-side runtime is required.

---

> **History:** earlier versions bundled an Express/MySQL backend for a contact form. The form was replaced by a static email and the backend was removed (2026-06-18). See [`docs/plans/`](./docs/plans/) for the change plans and the code review.
