// Runtime configuration for the static frontend.
// Override window.APP_CONFIG.backendUrl per environment (e.g. before deploy).
window.APP_CONFIG = window.APP_CONFIG || {
  backendUrl: "http://localhost:8000",
};
